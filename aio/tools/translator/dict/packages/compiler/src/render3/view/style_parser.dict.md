string representation of style as used in the `style` attribute in HTML.
  Example: `color: red; height: auto`.

HTML 中的 `style` 属性中使用的 style 的字符串表示。示例：`color: red; height: auto`。

An array of style property name and value pairs, e.g. `['color', 'red', 'height',
'auto']`

样式属性名称和值对的数组，例如 `['color', 'red', 'height', 'auto']`

Parses string representation of a style and converts it into object literal.

解析样式的字符串表示并将其转换为对象文字。